apiVersion: v1
kind: Service
metadata:
  name: {{ include "faiza-app.fullname" . }}
  labels:
    {{- include "faiza-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "faiza-app.selectorLabels" . | nindent 4 }}
